# Sample data (P01)

Place at least one **`.md`** or **`.pdf`** file here before running [`ingest.py`](../ingest.py) (see [P01 implementation plan](../../executions/implementation/P01-implementation-plan.md)).

The committed **[`sample.md`](./sample.md)** is enough for the [case study linear run](../../case-study/RUNBOOK.md) and **Ragas** batch rows grounded in Section C (PDF topic **text**).

**Optional stronger R1:** Add a small **`.pdf`** you may redistribute, then re-run **`ingest.py`**, so Unstructured partitions real PDF elements into **`multi_domain_docs`** (see [case-study data README](../../case-study/data/README.md)).

Use only content you may commit or keep local-only (add filenames to `.gitignore` if private).
