# Diagrams

- **`.mmd` files** — Mermaid source (text). Easy to review in diffs; render with [mermaid.live](https://mermaid.live) or your editor.
- **Exports** — Optional `.svg` / `.png` when you need static previews in contexts that do not run Mermaid.

Standard **five** diagram sources (`01`–`05`) — see `scaffold-architecture-README.md`. After scaffold, replace placeholders in each `.mmd` during Phase 1 (Architecture).
