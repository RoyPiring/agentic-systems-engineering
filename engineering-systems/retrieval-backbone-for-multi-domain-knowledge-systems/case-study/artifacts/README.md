# Case study artifacts (optional)

**Source of truth** for PASS evidence remains [`../executions/evidence/`](../executions/evidence/) and [`../validation/`](../validation/).

Use this folder only if you want **convenience copies** for reviewers (e.g. paste a short excerpt from `p04-ragas-batch.txt` after a local run). Do not treat this tree as authoritative unless files are explicitly maintained and named here.

| Suggested copy | Source |
| -------------- | ------ |
| *(none committed by default)* | — |
