name: AI PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - '**/*.md'
      - 'engineering-systems/**'

jobs:
  ai-review:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # LLM-based review is optional. Without secrets, nothing calls an API — there is no separate
      # deterministic "code review script" in this repo yet; humans use codereview.md + CHECKLIST.md.
      - name: Check for API Key
        id: check_key
        run: |
          if [ -z "${{ secrets.ANTHROPIC_API_KEY }}" ] && [ -z "${{ secrets.OPENAI_API_KEY }}" ]; then
            echo "has_key=false" >> $GITHUB_OUTPUT
            echo "No API key found. Skipping LLM review step."
          else
            echo "has_key=true" >> $GITHUB_OUTPUT
          fi

      - name: Manual review pointer (no API key)
        if: steps.check_key.outputs.has_key == 'false'
        run: |
          echo "::notice::No ANTHROPIC_API_KEY or OPENAI_API_KEY — LLM review skipped. Human review uses .github/code-review/codereview.md and CHECKLIST.md (and optional PRE_MERGE_REVIEW.md per slice)."

      - name: Run AI Review Script
        if: steps.check_key.outputs.has_key == 'true'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          # Wire LLM here: diff against base + system prompt from:
          #   .github/code-review/codereview.md  (code-reviewer — plan alignment, code quality)
          #   .github/code-review/CHECKLIST.md   (portfolio binary / rated gates)
          # gh pr comment $PR_NUMBER --body-file review-output.md
          echo "Automated review stub — attach codereview.md + CHECKLIST.md to the model context."
          BODY="**AI PR Review (stub)** — Review against [.github/code-review/codereview.md](https://github.com/${{ github.repository }}/blob/${{ github.head_ref }}/.github/code-review/codereview.md) and [.github/code-review/CHECKLIST.md](https://github.com/${{ github.repository }}/blob/${{ github.head_ref }}/.github/code-review/CHECKLIST.md)."
          gh pr comment "$PR_NUMBER" --body "$BODY"
